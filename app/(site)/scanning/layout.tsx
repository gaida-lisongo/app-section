const LayoutScanning = ({children}: {children: React.ReactNode}) => {
    return (
        <div className="w-full p-4 mt-20">
            {children}
        </div>
    );
};

export default LayoutScanning;