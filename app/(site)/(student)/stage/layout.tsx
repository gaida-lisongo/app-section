const LayoutStage = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <h1>Layout Stage</h1>
            {children}
        </div>
    );
};

export default LayoutStage;