const LayoutSujet = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <h1>Layout Sujet</h1>
            {children}
        </div>
    );
};

export default LayoutSujet;