const LayoutCourse = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            <h1>Layout Course</h1>
            {children}
        </div>
    );
};

export default LayoutCourse;
