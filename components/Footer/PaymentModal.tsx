const PaymentModal = ({
    matricule,
    isOpen,
    onClose
} : {
    matricule: string;
    isOpen: boolean;
    onClose: (payment: boolean) => void;
}) => {
    return (
        <div>
        </div>
    );
};

export default PaymentModal;
